// loop_sum.klx — 100M-iteration loop benchmark for the LLVM backend.
// Run: kylix build --backend=llvm [--llvm-opt=N] loop_sum.klx && ./loop_sum
program LoopBench;

var
  i, sum, n: Integer;

begin
  n := 100000000;
  sum := 0;
  for i := 1 to n do
    sum := sum + i;
  WriteLn(sum);
end.
